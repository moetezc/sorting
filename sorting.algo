ALGORITHM sorting
VAR
    i : INTEGER ; 
BEGIN
    FOR i FROM 1 TO arr.length -1 STEP 1  DO
        n := arr[i]; 
        
        WHILE ( arr[i-1] > n)) DO
            arr[i] := arr[i- 1];
            i := i-1; 
        END_WHILE
        arr[i] := n ; 
    END_FOR
END